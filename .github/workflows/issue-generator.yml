# This workflow manually generates GitHub Issues from the DEVELOPMENT_PLAN.md file.

name: "Generate Issues from Plan"

on:
  workflow_dispatch:
    inputs:
      phase_number:
        description: 'Phase number to generate issues for (e.g., 1)'
        required: true
        type: string

jobs:
  generate-issues:
    runs-on: ubuntu-latest
    permissions:
      issues: write # This permission is required to create issues.

    steps:
      - name: "Checkout code"
        uses: actions/checkout@v3

      - name: "Parse and Create Issues"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PHASE_INPUT: ${{ github.event.inputs.phase_number }}
        run: |
          echo "🚀 Starting issue generation for Phase $PHASE_INPUT..."

          # Corrected awk command to find the phase section without a space after "フェーズ".
          # It now looks for "### フェーズ" immediately followed by the number and a colon.
          PHASE_SECTION=$(awk -v phase="$PHASE_INPUT" '$0 ~ "^### フェーズ" phase ":" {flag=1; next} /^### フェーズ/ {flag=0} flag' DEVELOPMENT_PLAN.md)

          if [ -z "$PHASE_SECTION" ]; then
            echo "❌ Error: Could not find Phase $PHASE_INPUT in DEVELOPMENT_PLAN.md"
            exit 1
          fi

          # Extract tasks from the section and create issues
          echo "$PHASE_SECTION" | while IFS= read -r line; do
            # This looks for lines starting with "- [ ] **Task:**"
            if [[ $line == *"- [ ] "* ]]; then
              # Extract the title from the markdown task line
              title=$(echo "$line" | sed -e 's/- \[ \] \*\*Task:\*\* //' -e 's/\r//')

              # Determine the label based on task content
              label_list="Phase $PHASE_INPUT,enhancement"
              if [[ $title == *"(frontend)"* ]]; then
                label_list="$label_list,frontend"
                # Clean up title
                title=$(echo "$title" | sed 's/(frontend) //')
              fi

              echo "🤖 Creating issue: '$title'"

              # Create the issue using GitHub CLI
              gh issue create --title "$title" --body "This issue was automatically generated from the development plan for Phase $PHASE_INPUT." --label "$label_list"

              if [ $? -eq 0 ]; then
                echo "✅ Successfully created issue."
              else
                echo "❌ Failed to create issue. Please check token permissions."
              fi
            fi
          done

          echo "🎉 Issue generation finished!"
